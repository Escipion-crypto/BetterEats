{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "Keeps held items static while eating or drinking.",
  "authors": ["bettereats"],
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "client": [
      "bettereats.StaticHeldEatingClient"
    ]
  },
  "mixins": [
    "static_held_eating.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.16.0",
    "minecraft": ">=1.21.4 <=1.21.11",
    "fabric": "*"
  }
}
